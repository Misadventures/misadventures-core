import { CharacterGender } from "./character";

export const DEFAULT_NAME: string = "";
export const DEFAULT_GENDER: CharacterGender = CharacterGender.MALE;
export const DEFAULT_AGE: number = 0;

export const DEFAULT_HEALTH: number = 0;
